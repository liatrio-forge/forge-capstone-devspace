# Implementation Plans

Generated by the improve skill on 2026-07-02, against commit `595d158`.
Full-repo audit (standard depth): correctness, security, performance, tests,
tech debt, dependencies, DX, docs, and direction. Baseline at planning time:
87 tests passing, `go vet` clean, `gofmt` clean, coverage 61.9%.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| ---- | ----- | -------- | ------ | ---------- | ------ |
| 001 | Reject unsafe manifest project IDs (secrets path traversal) | P1 | S | — | DONE |
| 002 | Atomic writes for secrets, .env, and age identity | P1 | S | — | DONE |
| 003 | Safety-net tests: symlink escape + recipient listing | P1 | S | — | DONE |
| 004 | Validate manifest-supplied git remotes before clone | P2 | S | — | DONE |
| 005 | Hosted client: re-validate endpoint at use; env-var token | P2 | S | — | DONE |
| 006 | Fix mergeProject so rescans preserve user overrides | P1 | S | — | DONE |
| 007 | Bound hosted server's per-workspace mutex map (striped locks) | P2 | S | — | DONE |
| 008 | CI/Makefile lint + gofmt + govulncheck gates | P2 | S | — | DONE |
| 009 | Cross-process app-home locking for mutating commands | P2 | M | — | DONE |
| 010 | Scan: one project per monorepo, not per nested package.json | P2 | M | 006 | DONE |
| 011 | Watch: scoped refresh instead of full rescan per event | P3 | M | 009, 010 | DONE |
| 012 | `devspace project remove` (untrack + cascade) | P2 | M | 009 (soft) | DONE |
| 013 | SPIKE: manifest conflict reconciliation design + prototype | P3 | M | — | DRIFTED |
| 014 | SPIKE: access-role posture decision doc | P3 | M | — | DRIFTED |
| 015 | SPIKE: FUSE-capable CI go/no-go + mount backlog | P3 | M | — | READY |

Status values during SDD execution: TODO | READY | IN PROGRESS | DONE |
BRANCH (already implemented on `chore/hardening-pass`) | DRIFTED |
BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## SDD reconciliation notes

- Task 1 reconciliation ran all 15 source-plan drift checks against
  `595d158..HEAD`; every check returned no in-scope code drift.
- `chore/hardening-pass` contains overlapping branch work for Plans 005, 007,
  008, 013, and 014. Treat those commits as evidence to cherry-pick, rework,
  defer, or reject; do not merge the branch wholesale.
- Plans 001, 002, 003, and 006 were the first READY implementation group and
  are now DONE.
- Task 4 completed Plans 009, 010, 011, and 012 with app-home locking,
  monorepo scan descent, scoped watch refresh, project removal, race tests,
  smoke flows, and `make verify`.

## SDD execution notes

- Task 2 completed Plans 001, 002, 003, and 006 with targeted safety tests,
  coverage checks for recipient listing/export, and `make verify`.
- Task 3 completed Plans 004, 005, 007, and 008. Branch commits `e4f7bd0`,
  `71919e8`, and `9b4da48` were deferred; `973b6f1`, `70509e8`, and
  `ae5a61c` were rejected for this slice as out of source-plan scope.

## Dependency notes

- **010 after 006**: both change rescan behavior; 006's merge semantics must be
  settled so 010's regression tests aren't asserting against a moving target.
- **011 after 010**: scoped refresh reuses the scan's per-project body; the
  descent semantics must be final first. **011 after 009**: the refresh runs
  under the app lock.
- **012 after 009 (soft)**: the new command is a mutating entry point and
  should be wrapped in `withAppLock` if 009 landed; proceed without if not.
- **008 early is beneficial**: once lint gates exist, every later plan gets
  linted for free. It conflicts with nothing.
- **001 and 002 touch adjacent lines in `secrets.go`** (secretPath signature
  vs write calls) — execute serially, either order; each plan notes the
  expected drift from the other.
- Spikes 013/014/015 are independent of everything and of each other.
  013's doc must reference post-006 merge semantics if 006 has landed.

## Findings not planned this run (backlog)

- **Extract `hosted serve` HTTP lifecycle out of `commands.go`**
  (`commands.go:201-263` owns `http.Server`/signals/shutdown inline — the only
  untestable logic in the CLI layer; M effort). Worth doing alongside any
  future hosted-server work.
- **Dependabot/Renovate for `go.mod` + the pinned ko base-image digest**
  (`.goreleaser.yaml:27` pins `cgr.dev/chainguard/static:latest@sha256:…`;
  Chainguard GCs old digests, so a stale pin eventually breaks release builds
  with no warning; S effort, verify dependabot can track ko base images).
- **`docs/release-readiness.md` is stale** (line 60 claims "no hosted sync,
  daemon, FUSE layer" — all three shipped as prototypes; S effort: sync its
  language with `docs/capstone/case-study.md`, which is accurate).
- **README: document `DEVSPACE_HOME`/`DEV_DROP_HOME`** outside the
  troubleshooting section; mention `DEVSPACE_HOSTED_TOKEN` once Plan 005 lands.

## Findings considered and rejected

- **`PlanSync`/`ApplySync` "dead code"** (`workspace.go:306-320`): not fully
  dead — `ApplySync` is exercised by `devspace_test.go:179`; neither has a
  production caller (CLI calls `BuildPlan`/`ApplyLastPlan` directly). Verdict:
  harmless thin wrappers; delete opportunistically in a future refactor, not
  worth a plan.
- **Git/hosted sync push-flow duplication** (`workspace_sync.go:127-160` vs
  `hosted_sync.go:124-172`): real duplication, but unifying two backends with
  different transports/version models is a design task with MED risk to core
  conflict semantics. Not worth it unless a third backend appears or Plan 013's
  merge work forces shared conflict logic anyway.
- **`upsertProject` O(n) scan per project** (worst-case O(n²) per full scan):
  workspaces are tens of projects, not thousands. Micro-optimization; rejected.
- **Single-package layout**: deliberate MVP tradeoff per CLAUDE.md; the one
  concrete pain found is the `hosted serve` lifecycle item in the backlog above.
- **Direct dependency lag**: none — age/fsnotify/go-fuse/cobra/x-libs all
  current at planning time; only transitive drift (`x/crypto`, `pflag`), which
  `go mod tidy` picks up on the next direct bump.
- **`cloneRepo` argument injection**: already correctly guarded with `--`
  (`git.go:126`); remote *scheme* validation is Plan 004.
- **Hosted server auth/rate-limit design**: deliberately hardened contract
  with tests (`hardening_test.go`); only the mutex-map gap (Plan 007) was a
  real finding inside it.
